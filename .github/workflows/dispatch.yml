name: "Workflow Dispatch"

on:
  workflow_dispatch:
  push:
    branches:
      - master
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Github context
        run: |
          echo "${{ toJson(github) }}"
