name: "PR"

on:
  pull_request:
    types: 
      - opened
      - edited
      - closed
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Github context
        run: |
          echo "${{ toJSON(github) }}"
