name: "Setup sisusfox"
description: "Install sisusfox cli"
author: "Jorge López Rosende"
runs:
  using: composite
  steps:
    - name: Restore cache
      id: cache
      uses: actions/cache@v4
      with:
        path: /home/runner/work/_temp/_github_home/basic-memory
        key: memory
        restore-keys: |
          memory

    - name: Run docker action
      uses: ./actions/docker
